class Solution {
    public: 
    int scoreOfString(string s) {
        int sum = 0;
        for(int i=1;i<s.length();i++){
            int first  = (int) s.at(i - 1);
            int second = (int) s.at(i);
            sum += abs(first - second);
        }
        return sum;
    }
};
